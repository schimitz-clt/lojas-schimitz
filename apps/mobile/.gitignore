# Build
/.gradle/
/build/
/app/build/
/captures/
/.idea/
*.iml
.DS_Store
local.properties

# Keystores / signing (NUNCA commitar senhas ou keystores de release)
*.jks
*.keystore
keystore.properties
signing.properties
*.key

# Firebase (nunca commitar o arquivo real — use google-services.json.example)
/app/google-services.json
google-services.json

# Capacitor leftovers (se alguém experimentar)
/node_modules/
/.capacitor/
