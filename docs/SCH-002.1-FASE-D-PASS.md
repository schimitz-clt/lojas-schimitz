# SCH-002.1 — FASE D PASS

**Estado oficial:** FASE D **APROVADA** para liberar SCH-003 (somente readiness; SCH-003 NÃO iniciado nesta validação).

Data: 2026-09-08 (America/Sao_Paulo).

Ambiente: Postgres **local** `127.0.0.1:5432` / DB `lojas_schimitz_fase_d` apenas.  
**Nunca** `railway.internal` / produção.

---

## O que rodou

1. `DROP/CREATE DATABASE lojas_schimitz_fase_d` (superuser local `postgres` via sudo; owner `schimitz`)
2. `DATABASE_URL=postgresql://schimitz:***@127.0.0.1:5432/lojas_schimitz_fase_d?schema=public`
3. `npx prisma migrate deploy` — **15/15 migrations aplicadas** em DB limpo
4. `npx tsx prisma/seed.ts` — admin + catálogo seed
5. Specs existentes relacionados (unit + db)
6. Integração real Nest+Postgres: `apps/api/src/modules/orders/fase-d.db.spec.ts` (via **dist compilado** — metadata de DI)

---

## Resultados por item

| # | Item | Resultado | Evidência |
|---|------|-----------|-----------|
| 1 | `prisma migrate deploy` em DB limpo | **PASS** | 15 migrations aplicadas; recheck: no pending |
| 2 | Seed | **PASS** | `admin@lojas-schimitz.test` + produtos/inventory |
| 3 | `*.db.spec.ts` inventory/orders (+ related) | **PASS** | `inventory.db.spec`, `tracking.db.spec`; também concurrency/idempotency/reservations-expiry/reservation unit |
| 4 | Concorrência real: 2 creates na última unidade (CAS) | **PASS** | 1 win + 1 `INSUFFICIENT_STOCK`; `qtyOnHand=1`, `qtyReserved=1` (sem negativo) |
| 5 | Expiração real de reserva | **PASS** | `awaiting_payment` com `expiresAt` no passado → `ReservationsExpiryService.tick()` → status `cancelled`, stock liberado |
| 6 | Idempotency-Key | **PASS** | mesma key+payload → mesmo `order.id`; addressId diferente → `IDEMPOTENCY_KEY_REUSED` |
| 7 | Rollback/failure | **PASS** | estoque pré-reservado → create falha; sem order órfã; stock não oversold |

---

## Comandos (reprodução local)

```bash
export DATABASE_URL='postgresql://schimitz:schimitz@127.0.0.1:5432/lojas_schimitz_fase_d?schema=public'
export JWT_ACCESS_SECRET='…' JWT_REFRESH_SECRET='…' PAYMENTS_PROVIDER=null

# safety
echo "$DATABASE_URL" | grep -Ei 'railway|rlwy|\.internal' && exit 1

cd /workspace/lojas-schimitz
npx prisma migrate deploy --schema=prisma/schema.prisma
npx tsx prisma/seed.ts

cd apps/api
npx tsc -p tsconfig.json
node -e "require('reflect-metadata'); require('./dist/modules/orders/fase-d.db.spec.js')"

npx tsx src/modules/inventory/inventory.db.spec.ts
npx tsx src/modules/orders/tracking.db.spec.ts
npx tsx src/modules/orders/concurrency.spec.ts
npx tsx src/modules/orders/idempotency.spec.ts
npx tsx src/modules/orders/reservations-expiry.spec.ts
npx tsx src/modules/inventory/inventory.reservation.spec.ts
```

> Nota: `fase-d.db.spec.ts` deve rodar via **`tsc` + `node dist/...`** (ou runtime com `emitDecoratorMetadata`). `tsx` sozinho não emite metadata de DI do Nest — `ShippingService.prisma` fica `undefined`.

---

## Arquivos alterados nesta validação

- `apps/api/src/modules/orders/fase-d.db.spec.ts` (**novo** — harness de prova real)
- `docs/SCH-002.1-FASE-D-PASS.md` (**novo**)
- `docs/SCH-002.1-FASE-D-CHECKPOINT.md` (atualizado)

Sem mudanças de lógica de negócio necessárias: CAS / expiry / idempotency já estavam corretos no código.

---

## FASE D APPROVED FOR SCH-003

**SIM — FASE D APPROVED FOR SCH-003.**

Próximo passo autorizado pelo produto: iniciar SCH-003 (Mercado Pago / PIX / webhook) **somente** após este PASS.  
Esta sessão **não** inicia implementação SCH-003 além de documentar readiness (`PAYMENTS_PROVIDER=null` validado no path de orders).

### Blockers remanescentes antes de SCH-003 (ops/produto, não Fase D)

- Credenciais Mercado Pago de **teste** (nunca produção) e webhook secret em ambiente isolado
- Decisão de provedor efetivo (`PAYMENTS_PROVIDER=mercadopago` só em staging/test)
- Não apontar `DATABASE_URL` de validação para Railway/produção
- Health/secrets (Fase E) ainda opcional conforme roadmap SCH-002.1

---

## Safety checklist

- [x] Sem deletes em produção
- [x] Sem cobranças reais Mercado Pago (`PAYMENTS_PROVIDER=null`)
- [x] Sem secrets no relatório
- [x] SCH-003 feature work não iniciado além deste checkpoint
