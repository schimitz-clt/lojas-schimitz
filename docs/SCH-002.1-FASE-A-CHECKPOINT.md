# SCH-002.1 — FASE A CHECKPOINT

**Estado oficial:** SCH-002.1 — FASE A VALIDADA EM POSTGRESQL 16 VAZIO.

Documento de continuidade. Não é prova de Fases B–G. Não autoriza SCH-003.

Data da validação: 2026-08-26 / 2026-08-27 UTC (sessão de implementação + validação).

---

## O que este checkpoint afirma

Somente fatos executados nesta sessão sobre a árvore de trabalho
`/home/workdir/artifacts/lojas-schimitz`.

## O que este checkpoint não afirma

- Docker build / compose **não** rodou.
- FASE B **não** iniciada.
- SCH-003 / Mercado Pago / PIX / webhook / frete real **não** implementados.
- A vitrine `lojas-schimitz-index.html` **não** faz parte deste núcleo.
- Nenhum ZIP novo foi gerado.

---

## Fontes

| Item | Fato |
|---|---|
| ZIP original | `/home/workdir/attachments/lojas-schimitz-sch002.1.zip` — **82.567 bytes** — não sobrescrito |
| Cópia de segurança pré-Fase A | `/home/workdir/artifacts/lojas-schimitz-sch002.1.zip.pre-fase-A.bak` — 82.567 bytes |
| Árvore de trabalho | `/home/workdir/artifacts/lojas-schimitz` |
| PostgreSQL | usado **apenas para validação**; não é ambiente de produção |

---

## Cadeia de migrations (aplicada de verdade)

Ordem em banco vazio `lojas_schimitz_fase_a`:

1. `20260825_sch001_baseline`
2. `20260826_sch002`
3. `20260826_sch002_1`
4. `20260826_sch002_1_5`

Comando: Prisma 6.3.1 `migrate deploy --schema=prisma/schema.prisma`  
Resultado: **4 migrations aplicadas** (`_prisma_migrations.finished_at` preenchido).

Motor: **PostgreSQL 16.15** (Ubuntu `postgresql-16`).  
Docker: binário **inexistente** — **NÃO EXECUTADO**. Não declarar Docker como PASSOU.

---

## Banco após migrate

| Item | Fato |
|---|---|
| Tabelas de negócio | **17** (`User`, `RefreshToken`, `Address`, `Category`, `Product`, `ProductImage`, `Inventory`, `Cart`, `CartItem`, `Coupon`, `Order`, `OrderItem`, `Payment`, `Review`, `Favorite`, `AuditLog`, `IdempotencyRecord`) |
| Tabela Prisma | `_prisma_migrations` |
| Foreign keys | **18** |
| CHECKs em `Inventory` | `Inventory_qtyOnHand_nonneg`, `Inventory_qtyReserved_nonneg`, `Inventory_reserved_lte_onhand` |
| Unique pedido + idempotência | `Order_userId_idempotencyKey_key` |

---

## Seed

`ADMIN_EMAIL=admin@lojas-schimitz.test` + `prisma/seed.ts`  
Saída: `Seed SCH-001 concluído.`

| Item | Fato |
|---|---|
| Seed | **PASSOU** |
| Admin | `admin@lojas-schimitz.test` / role `admin` / status `active` |
| Inventory | 6 linhas; `qtyReserved` = 0 em todas; `qtyOnHand` entre 5 e 30 |

---

## CHECKs testados no PostgreSQL

| Teste | Resultado |
|---|---|
| `qtyOnHand = -1` | rejeitado — `Inventory_qtyOnHand_nonneg` — **PASSOU** |
| `qtyReserved = -1` | rejeitado — `Inventory_qtyReserved_nonneg` — **PASSOU** |
| `qtyReserved = qtyOnHand + 1` | rejeitado — `Inventory_reserved_lte_onhand` — **PASSOU** |
| `qtyReserved = 0` com `qtyOnHand = 12` | aceito |

---

## CAS

`apps/api/src/modules/inventory/inventory.service.ts`

- MD5: `6078332dee967cde34055dc02bdc779a`
- Tamanho: 2587 bytes
- Idêntico ao ZIP original
- Predicado intacto: `AND ("qtyOnHand" - "qtyReserved") >= ${qty}`

**CAS: INTACTO**

---

## Arquivos da Fase A (referência)

Criados na implementação (não nesta escrita, exceto este checkpoint):

- `prisma/migrations/20260825_sch001_baseline/migration.sql`
- `prisma/migrations/20260826_sch002_1_5/migration.sql`
- `prisma/migrations/migration_lock.toml` (`provider = "postgresql"`)

Modificados na implementação:

- `prisma/seed.ts` (default admin `admin@lojas-schimitz.test`)
- `.env.example` (`ADMIN_EMAIL=admin@lojas-schimitz.test`)

Históricos **não** alterados:

- `prisma/migrations/20260826_sch002/migration.sql`
- `prisma/migrations/20260826_sch002_1/migration.sql`
- `apps/api/src/modules/inventory/inventory.service.ts`

Este arquivo: `docs/SCH-002.1-FASE-A-CHECKPOINT.md` (criado no checkpoint).

---

## Próximo passo (não executado)

FASE B — idempotência. **Aguarda autorização explícita.**

SCH-003 permanece **BLOQUEADO**.
