{
  "name": "lojas-schimitz",
  "private": true,
  "version": "0.1.0",
  "description": "Plataforma Lojas Schimitz — SCH-001 Fundação",
  "devDependencies": {
    "prisma": "^6.19.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "scripts": {
    "build": "cd apps/api && npm install && npx prisma generate --schema=../../prisma/schema.prisma && npm run build",
    "start": "cd apps/api && npx prisma migrate deploy --schema=../../prisma/schema.prisma && node dist/main.js"
  }
}
